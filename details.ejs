<!DOCTYPE html>
<html lang="en">
  <head>
    <title>Details</title>
  </head>
  <body>
    <h1>List of Registered Names</h1>

    <table border="1" width="50%">
      <tr>
        <th>Name</th>
      </tr>

      <% if(data.length>0){ %> 
        <% for(var i=0;i<data.length;i++){ %>
          <tr>
            <td><%= data[i].name %></td>
          </tr>
        <% } %>
        <% }else{ %>
            <h2>No entry</h2>
            <% } %>
      
      
    </table>
  </body>
</html>
